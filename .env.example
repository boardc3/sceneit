# Gemini API (required)
GOOGLE_API_KEY=your_google_api_key

# Vercel Blob (required - used for image and metadata storage)
BLOB_READ_WRITE_TOKEN=your_blob_token

# Admin Dashboard (required for /admin)
ADMIN_PASSWORD=your_admin_password
