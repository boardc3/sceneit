// No database setup needed — metadata is stored as JSON in Vercel Blob.
// Blob files are created automatically on first write.
console.log('No setup needed. Metadata blobs are created on first use.');
